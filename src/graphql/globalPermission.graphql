fragment DefaultGlobalPermissionInfo on GlobalPermission {
    id
    name
    description
    entries
    allUsers
    users {
        totalCount
        nodes {
            id
            name: displayName
        }
    }
}

