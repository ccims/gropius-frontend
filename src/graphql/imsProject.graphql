fragment DefaultIMSProjectInfo on IMSProject {
    id
    name
    description
    ims {
        id
        name
        template {
            id
            name
            description
        }
    }
    ...DefaultSyncPermissionTargetInfo
}
