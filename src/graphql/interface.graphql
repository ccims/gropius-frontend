fragment DefaultInterfaceDefinitionInfo on InterfaceDefinition {
    id
    visibleSelfDefined
    invisibleSelfDefined
    visibleDerivedBy {
        totalCount
    }
    invisibleDerivedBy {
        totalCount
    }
}

