fragment DefaultUserInfo on User {
    id
    username
    displayName
    avatar
}

fragment CurrentUserInfo on User {
    ...DefaultUserInfo
    email
    ... on GropiusUser {
        isAdmin
    }
}

